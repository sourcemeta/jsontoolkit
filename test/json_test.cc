#include <gtest/gtest.h>
#include <jsontoolkit/json.h>

TEST(JSON, XXXXXXX) { EXPECT_TRUE(true); }

int main(int argc, char **argv) {
  ::testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}
